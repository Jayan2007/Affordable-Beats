<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Payment Settings & Order Management</title>
    <style>
        :root {
            --bg-light: #ffffff;
            --bg-dark: #121212;
            --text-light: #000000;
            --text-dark: #ffffff;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: var(--bg-light);
            color: var(--text-light);
            margin: 0;
            padding: 0;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background-color: #f5f5f5;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
        }

        .nav-buttons button {
            margin-left: 10px;
        }

        .section {
            padding: 40px 20px;
        }

        .section h2 {
            margin-bottom: 20px;
        }

        #theme-toggle {
            cursor: pointer;
        }

        footer {
            text-align: center;
            padding: 20px;
            background-color: #f5f5f5;
        }

        /* Payment Settings & Order Management Styles */
        .payment-settings-section {
            padding: 40px 20px;
            background-color: #f9f9f9;
        }

        .order-card {
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        .order-stat {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .order-btn {
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            cursor: pointer;
        }

        .order-btn:hover {
            background-color: #0056b3;
        }

        /* Payment Settings Form Styles */
        .payment-settings-form {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        .payment-settings-form input {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">Affordable Beats - Admin Dashboard</div>
        <div class="nav-buttons">
            <button onclick="location.href='admin-login.html'">Logout</button>
            <button id="theme-toggle">Toggle Theme</button>
        </div>
    </header>

    <div class="banner" id="main-banner">
        <!-- Background image goes here: replace with an image named 'banner-placeholder.jpg' -->
    </div>

    <!-- Payment Settings & Order Management Section -->
    <div class="payment-settings-section" id="payment-settings">
        <h3>Payment Settings & Order Management</h3>

        <!-- Order List -->
        <div class="order-card">
            <h4>All Orders</h4>
            <div class="order-stat">
                <span>Order #1</span>
                <span>Customer: John Doe</span>
                <span>Status: Completed</span>
                <span>Amount Paid: $5</span>
                <button class="order-btn">Mark as Shipped</button>
            </div>
            <div class="order-stat">
                <span>Order #2</span>
                <span>Customer: Jane Smith</span>
                <span>Status: Pending</span>
                <span>Amount Paid: $20</span>
                <button class="order-btn">Mark as Completed</button>
            </div>
        </div>

        <!-- Payment Settings Form (Placeholder for your settings) -->
        <div class="payment-settings-form">
            <h4>Payment Settings</h4>
            <form action="/update-payment-settings" method="POST">
                <label for="paypal-email">PayPal Email (leave blank for now):</label>
                <input type="email" id="paypal-email" name="paypal-email" placeholder="Enter PayPal email" required>

                <label for="currency">Currency (USD):</label>
                <input type="text" id="currency" name="currency" value="USD" disabled>

                <button type="submit" class="order-btn">Update Settings</button>
            </form>
        </div>
    </div>

    <footer>
        &copy; 2025 Affordable Beats - Admin Dashboard
    </footer>

    <script>
        const toggle = document.getElementById('theme-toggle');
        toggle.addEventListener('click', () => {
            const body = document.body;
            if (body.style.backgroundColor === 'var(--bg-dark)') {
                body.style.backgroundColor = 'var(--bg-light)';
                body.style.color = 'var(--text-light)';
            } else {
                body.style.backgroundColor = 'var(--bg-dark)';
                body.style.color = 'var(--text-dark)';
            }
        });
    </script>
</body>
</html>
